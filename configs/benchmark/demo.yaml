# Clinical decision-making workflow demo configuration

# Path to benchmark data JSON file
benchmark_data_path: database/output/benchmark_data.json

# Which case to use from the benchmark (index in the cases array)
case_index: 1  # Uses second case (index 1): 59M with abdominal pain -> pancreatitis

show_tool_calls: true
